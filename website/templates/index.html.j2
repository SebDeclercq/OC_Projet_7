{% extends "bootstrap/base.html" %}

{% block title %}
Bienvenue chez GrandPy !
{% endblock %}

{% block styles %}
{{ super() }}
<link rel="stylesheet" type="text/css" href="{{ url_for('static', filename='grandpy.css')}}">
{% endblock %}

{% block content %}
<div class="container mt-5">
    <div class="row">
        <div class="col col-lg">
            <h3>Salut mon gars !</h3>
        </div>
    </div>
    <div class="row row-lg mt-5">
        <div class="col col-lg">
            <form method="POST">
                <div class="form-group">
                    <label for="ask-grandpy">
                        Alors, dis à GrandPy !
                    </label>
                    <div class="input-group">
                        <input type="text" class="form-control" id="ask-grandpy"
                            placeholder="Que veux-tu savoir aujourd'hui ?">
                        <div class="input-group-append">
                            <button class="btn btn-outline-primary" type="button" id="ask">Vas-y !</button>
                        </div>
                    </div>
                </div>
            </form>
        </div>
    </div>
</div>
{% endblock %}
